/* strings */
let hero = 'King Arthur'; // string (spaces are allowed)
let hero2 = 'King  Arthur'; // string (this is a different string than the one above because of the extra space)
let weaponDescription = "King Arthur's sword is Excalibur"; // string (single quotes are allowed inside double quotes)

/* numbers */
let piecesOfGold = 100; // positive integer
let ouncesOfGoldCoin = 14.2; // positive decimal number
let coinSatchel = -5; // negative integer
let buriedTreasureInInches = -12.5; // negative decimal number

/* booleans */
let isHeroGood = true; // boolean value of true
let isVillianGood = false; // boolean value of false

/* undefined */
let buriedTreasureLocation; // variable declared but not initialized, so it is undefined
let herosFate = undefined; // variable explicitly set to undefined

/* null */
let herosWeapon = null; // variable explicitly set to null, meaning it has no value

console.log(buriedTreasureLocation);